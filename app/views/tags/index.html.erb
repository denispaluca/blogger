<h1>ALl Tags</h1>

<ul id="articles">
  <% @tags.each do |tag| %>
    <li>
      <%= link_to tag.name, tag_path(tag)%>
    </li>
  <% end %>
</ul>

<%= link_to "Create a New Article",new_article_path,class: "new_article" %>
